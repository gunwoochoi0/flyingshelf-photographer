# Canvas Render Service - Environment Variables

# Server Configuration
PORT=3000

# CORS Configuration
# Default: allow all origins
ALLOWED_ORIGINS=*
# To restrict to specific origins, use comma-separated list:
# ALLOWED_ORIGINS=https://yourdomain.com,https://app.yourdomain.com,http://localhost:3001

# API Secret for bypassing CORS (server-to-server authentication)
# SECURITY: Change this to a strong random value in production!
# Generate a secure secret: openssl rand -hex 32
API_SECRET=your-secret-key-change-this
